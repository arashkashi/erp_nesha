<%= form_with(model: production_raw_store) do |form| %>
  <% if production_raw_store.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(production_raw_store.errors.count, "error") %> prohibited this production_raw_store from being saved:</h2>

      <ul>
        <% production_raw_store.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label t('name') %>
    <%= form.text_field :name %>
  </div>

  <div class="field" style="display: none">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, value: current_user.id %>
  </div>

  <div class="actions">
    <%= form.submit t('record') %>
  </div>
<% end %>
