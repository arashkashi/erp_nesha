<p id="notice"><%= notice %></p>

<h1>Truck Scale Outputs</h1>

<table>
  <thead>
    <tr>
      <th>Order</th>
      <th>Amount</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @truck_scale_outputs.each do |truck_scale_output| %>
      <tr>
        <td><%= truck_scale_output.order_id %></td>
        <td><%= truck_scale_output.amount %></td>
        <td><%= truck_scale_output.user_id %></td>
        <td><%= link_to 'Show', truck_scale_output %></td>
        <td><%= link_to 'Edit', edit_truck_scale_output_path(truck_scale_output) %></td>
        <td><%= link_to 'Destroy', truck_scale_output, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Truck Scale Output', new_truck_scale_output_path %>
