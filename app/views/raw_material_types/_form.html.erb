<%= form_with(model: raw_material_type) do |form| %>
  <% if raw_material_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(raw_material_type.errors.count, "error") %> prohibited this raw_material_type from being saved:</h2>

      <ul>
        <% raw_material_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
