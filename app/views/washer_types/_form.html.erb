<%= form_with(model: washer_type) do |form| %>
  <% if washer_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(washer_type.errors.count, "error") %> prohibited this washer_type from being saved:</h2>

      <ul>
        <% washer_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
