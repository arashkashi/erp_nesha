<%= form_with(model: order_item) do |form| %>
  <% if order_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order_item.errors.count, "error") %> prohibited this order_item from being saved:</h2>

      <ul>
        <% order_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label t('type') %>
    <%= form.collection_select :pipe_type_id, PipeType.all, :id, :name %>
  </div>

  <div class="field">
    <%= form.label t('meter', default: "Amount (m)") %>
    <%= form.text_field :amount %>
  </div>

  <div class="field">
    <%= form.label t('project') %>
    <%= form.collection_select :order_id, Order.all, :id, :name %>
  </div>

  <div class="actions">
    <%= form.submit t('record', default: 'Record')%>
  </div>
<% end %>
