<%= form_with(model: washer_input) do |form| %>
  <% if washer_input.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(washer_input.errors.count, "error") %> prohibited this washer_input from being saved:</h2>

      <ul>
        <% washer_input.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :washer_type_id %>
    <%= form.text_field :washer_type_id %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
