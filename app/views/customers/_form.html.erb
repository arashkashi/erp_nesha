<%= form_with(model: customer) do |form| %>
  <% if customer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(customer.errors.count, "error") %> prohibited this customer from being saved:</h2>

      <ul>
        <% customer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :phone %>
    <%= form.text_field :phone %>
  </div>

  <div class="field">
    <%= form.label :meta %>
    <%= form.text_field :meta %>
  </div>

  <div class="field">
    <%= form.label User.find(session[:user_id]).name %>
    <%= form.text_field :user_id, value: User.find(session[:user_id]).id , readonly: true %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
